services:
  - type: web
    name: forge2-qualifier-chaman-api
    # Docker runtime — Render builds from the Dockerfile in backend/
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    # Render free tier
    plan: free
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: APP_KEY
        generateValue: true
      - key: APP_URL
        sync: false          # fill in after first deploy with the Render URL
      - key: DB_CONNECTION
        value: sqlite
      - key: DB_DATABASE
        value: /var/data/database.sqlite
      - key: FRONTEND_URL
        value: https://forge2-qualifier-chaman.vercel.app
      - key: LOG_CHANNEL
        value: stderr
      - key: CACHE_DRIVER
        value: file
      - key: SESSION_DRIVER
        value: file
      - key: QUEUE_CONNECTION
        value: sync
    disk:
      name: sqlite-data
      mountPath: /var/data
      sizeGB: 1
    healthCheckPath: /up
